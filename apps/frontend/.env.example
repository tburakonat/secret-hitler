# Normally not needed: dev uses the Vite proxy, production the nginx proxy
# (both same-origin). Only set this for split deployments where the backend
# lives on a different origin.
# VITE_BACKEND_URL=https://your-backend.example.com
